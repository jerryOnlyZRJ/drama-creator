# 发布验证范围

验证日期：2026-09-19。此记录针对当前公开源码候选副本，不继承旧仓库的历史验收结论。

## 已验证

- macOS arm64，Node.js 24.21.0，npm 11.19.0：使用 npm 官方 registry 从锁文件重新安装成功，keytar 原生模块可加载。
- 更新构建依赖后全量 354 项测试通过，0 失败、0 跳过；额外使用系统 sandbox 阻止测试访问作者真实应用数据目录。
- 前端构建与 360 KB 包体门禁通过。相对 Markdown 链接检查没有发现断链。
- 本地浏览器 1280×720：项目库打开独立演示项目，保存核心立意，刷新后仍存在，并与磁盘文件回读一致；未记录浏览器错误或警告。
- Rust 1.98.1、macOS 26.5 SDK：Tauri Apple Silicon 应用构建完成。应用包内置服务可直接执行，返回内容与打包首页字节一致。
- Gitleaks 8.30.1 对最终逐文件源码清单扫描无命中。工具扫描不保证不存在全部敏感数据。

- 真实 FFmpeg 对两个测试色块/正弦波视频完成拼接、SRT 生成及字幕烧录，ffprobe 读取到 H.264 视频；这些合成输入不代表作品或模型质量。
- 两张公开预览来自隔离的作品展示项目，包含作者允许展示的《后来，玫瑰盛开》素材，原始视口截图未经图像编辑。

## 模拟与未验证边界

测试中的模型返回、凭据及部分媒体文件为 fixture/stub，部分时间固定；这些测试不证明外部平台生成或真实账号授权成功。浏览器运行检查使用真实服务与临时磁盘目录，但只覆盖项目打开与保存。

最终隐私文案调整后再次完成桌面构建；原内置服务运行检查发生于前一构建，最终桌面窗口未启动验证。桌面包仅为本机构建验证产物，当前为 ad-hoc 签名，未经 Apple 公证，不作为正式签名安装包发布。尚未验证 Intel、Windows 或 Linux 桌面包。GitHub Actions 配置已提供，但尚未在远端执行。

首次源码发布只包含源代码、文字文档和审核后的应用资源，不包含作者作品、旧 Git 历史、依赖缓存、编译目录或本地验证日志。第三方生成能力须由使用者自行配置并实际验证。

## 构建环境说明

本机默认 macOS 27 SDK 与当前链接器组合会报 unknown architecture。验证构建显式选择本机已有的 macOS 26.5 SDK，未改动系统默认 SDK。其他机器应使用自身兼容的 Xcode/Command Line Tools，不应照搬本机绝对路径。
